import { TestBed } from '@angular/core/testing';

import { PrelevementAllerResolver } from './prelevement-aller.resolver';

describe('PrelevementAllerResolver', () => {
  let resolver: PrelevementAllerResolver;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    resolver = TestBed.inject(PrelevementAllerResolver);
  });

  it('should be created', () => {
 //   expect(resolver).toBeTruthy();
  });
});
